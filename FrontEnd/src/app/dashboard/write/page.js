"use client"
import TextEditor from '@/components/TextEditor/TextEditor';
import React from 'react';

const page = () => {
    return (
        <div>
            <TextEditor/>
        </div>
    );
};

export default page;